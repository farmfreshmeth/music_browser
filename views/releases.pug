extends layout

block content
  h1 #{folder}
  p #{JSON.stringify(pagination)}

  each release in releases
    .pure-g
      .pure-u-1-3
        //- img(src=release['thumb'])
      .pure-u-1-3
        div #{release['artists_sort']}
        a(href="/release/" + release['id']) #{release['title']}
      .pure-u-1-3
        //- pre #{JSON.stringify(release, null, 2)}

  pre
    #{JSON.stringify(releases, null, 2)}